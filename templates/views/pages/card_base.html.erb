<%# DeleteThis - options:
  stacked: true / false; defaults to false
  used less often (but still available):
    max_height: any max-height css value; defaults to 100%
    min_height: any min-height css value; defaults to auto
    height: any height css value; defaults to auto
    width_class: any tailwind classes; defaults to nil.
%>
<%= card do %>
  <%# DeleteThis - for custom header, use: ,hche (Html Card HEader) %>
  <%= card_header title: 'ChangeTitle', subtitle: 'ChangeSubTitle' %>
  <%= card_body do %>
  <% end %>
<% end %>
