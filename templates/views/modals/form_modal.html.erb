<%= modal id: "ChangeModel-#{ChangeModel.id}-ChangeSubControllerNameOrRemoveThis-ChangeActionOrDescription-modal" do %>
  <%= content_tag 'DIV', nil, id: "ChangeModalId-flash", class: 'flash-messages' %>
  <%# DeleteThis - add subtitle with this option - subtitle: 'ChangeSubTitle' %>
  <%= card_header title: 'ChangeHeader' %>
  <%= card_body do %> 
    <%# DeleteThis - if form does not use an instance of a active record model, then:
        replace model: with scope:
        add value: option to inputs as needed
        or replace "f.inline_errors" with "inline_errors" or remove entirely
    %>
    <%= form_with model: ChangeModel, url: ChangePath(account, ChangeModel), method: :ChangeHtmlMethod do |f| %>
      <%# DeleteThis - insert hidden fields here if necessary %>

      <%# DeleteThis - insert form groups (or fields for if necessary) %>

      <div class="form-submit-buttons">
        <button type="button" class="btn btn-secondary" data-action="click->modal#hide">Cancel</button>
        <%= f.submit 'AddOrSave', class: 'btn' %>
      </div>
    <% end %> 
  <% end %> 
<% end %>
